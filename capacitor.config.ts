import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flash.multimarcas',
  appName: 'FlashMultimarcas',
  webDir: 'out',
  android: {
    backgroundColor: '#000000',
  },
};

export default config;
